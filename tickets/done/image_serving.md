# Ticket: Image Serving — moved

This ticket moved to `price-comparison-web/tickets/image_serving.md`.

The serving layer (CloudFront distribution) is owned by `price-comparison-web/infra/`.
The S3 bucket remains in this repo — no Terraform changes needed here.
