# Ticket: Minimal Product Read API — migrated

**Migrated to `../price-comparison-web/tickets/product_api.md`**

The FastAPI code and local Makefile target live in `price-comparison-web`.
Cross-repo work items (export script, Lambda Terraform, IAM) remain in
`ecommerce-scraper` — see the ticket in `price-comparison-web` for the full
work item table.
